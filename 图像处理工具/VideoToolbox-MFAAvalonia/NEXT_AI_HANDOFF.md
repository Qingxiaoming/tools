# Next AI Handoff

## 1) 项目与目标

- 主项目：`VideoToolbox-MFAAvalonia`
- 原项目（对照）：`图像处理工具/视频工具箱`（Python Tk）
- 当前目标：功能已基本移植，后续以“UI 打磨 + 回归验证 + 文档收口”为主。

## 2) 当前已完成（高层）

- UI 同款化主线 `UI-01` 到 `UI-07` 已完成。
- 拖拽导入、跨页传递、日志面板、主题切换、托盘行为已接入。
- 窗口行为：
  - 隐藏系统白色标题栏
  - 最小化/关闭进托盘
  - 托盘双击切换显示/隐藏
  - 托盘可开关“拖拽移动窗口”（默认关闭）

## 3) 待下个 AI 处理（按优先级）

### P0

1. `UI-10` 功能回归验证（必须）
   - 截取 / 裁剪（含 ROI）/ 合并（含排序与音频）/ 文档生成与转运 / 录屏整理（含排序）
   - 输出完整回归记录到 `UI_REFACTOR_LOG.md`

2. 行为一致性复核（原 Python vs Avalonia）
   - 重点看“仅拖拽输入”后的用户路径是否顺手
   - 重点看边界场景（空列表、非法输入、文件不存在）

### P1

3. 布局紧凑度二次优化
   - 提供“紧凑/标准”两档密度（间距、按钮高度、列表高度）
   - 避免再次引入过多说明文本导致空旷

4. 交互一致性
   - 统一各页路径输入区域的视觉语言（当前已向“仅拖拽 + 紧凑列表/文本”收敛）
   - 统一按钮主次层级

### P2

5. 可选：在状态栏增加“当前模式/当前页提示”弱提示
6. 可选：增加更细粒度托盘动作（例如“仅显示窗口，不抢焦点”）

## 4) 本轮用户偏好（重要）

- 交流语言：中文
- 用户更偏好“先看效果”，近期要求：
  - 界面改完后**默认不做编译检查**
  - 减少冗余说明文字，优先紧凑布局

## 5) 关键文档

- 变更日志：`UI_REFACTOR_LOG.md`
- 待办总表：`UI_TODO.md`

