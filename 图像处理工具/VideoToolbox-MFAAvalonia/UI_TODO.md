# VideoToolbox UI 同款化 TODO

目标：将 `VideoToolbox-MFAAvalonia` 的界面风格对齐到接近 `MAA/MFAAvalonia` 的视觉与交互体验，同时保持现有业务逻辑与 ffmpeg 行为不变。

## 当前状态（用于交接）

| ID | 优先级 | 任务 | 具体动作 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| UI-01 | P0 | 建立主题层 | 新增并接入 `Styles/Theme.axaml`（或同等主题资源），在 `App.axaml` 注册全局样式 | 应用启动后全局控件样式统一生效 | 已完成 |
| UI-02 | P0 | 主窗口结构同款化 | 重做 `Views/MainWindow.axaml`：左导航 + 右内容区 + 日志区（底部或侧边） | 结构视觉与 MAA/MFA 形态一致，不再是朴素 Tab 页 | 已完成 |
| UI-03 | P0 | 导航与分区卡片化 | 将原 Tab 内容改为分区卡片布局（输入区/输出区/操作区） | 每个模块信息层级清晰，间距/圆角/边框统一 | 已完成 |
| UI-04 | P1 | 状态反馈增强 | Busy/Idle 时按钮禁用、加载态、进度提示、错误状态色 | 执行任务时有明确视觉反馈，无“点了没反应” | 已完成 |
| UI-05 | P1 | 日志面板增强 | 日志区支持自动滚动、清空、可折叠（至少前两项） | 连续日志可读性提升，操作成本降低 | 已完成 |
| UI-06 | P1 | 主题细节对齐 | 统一字号、主色、警告色、成功色、控件高度与间距体系 | 不同页面视觉语言一致 | 已完成 |
| UI-07 | P1 | 深浅色能力预留 | 预留主题切换入口或资源分层（可先只启用一种主题） | 后续可低成本加深浅切换 | 已完成 |
| UI-08 | P0 | 保持业务不变 | 不改 ffmpeg 参数构建与业务流程逻辑，仅做 UI/交互层改造 | 五大功能行为与当前一致 | 基本完成（建议回归确认） |
| UI-09 | P0 | 按需构建校验 | 提交前或用户明确要求时执行 `dotnet build -c Debug` + lints 检查 | 按需执行且保持 0 error（理想 0 warning） | 持续 |
| UI-10 | P0 | 回归验证 | 点通 5 个功能入口：截取/裁剪/合并/文档/录屏整理 | 各入口可达且不崩溃 | 已完成（代码链路回归） |
| UI-11 | P2 | 交接文档持续更新 | 维护 `UI_REFACTOR_LOG.md` 和 `NEXT_AI_HANDOFF.md` | 下一个 AI 接手无断层 | 已完成（当前轮） |
| UI-12 | P0 | 自动化冒烟回归脚本 | 新增 `tests/VideoToolbox.SmokeTests` 与 `run_ui_smoke_tests.ps1`，覆盖 5 入口命令可达、空输入防呆、拖拽分发 | 可一键执行并快速发现明显回归 | 已完成 |

## 下一步建议（留给下一个 AI）

1. 继续细化页面视觉一致性（按钮主次层级、分区对齐、弱提示风格）。
2. 添加可扩展的配色方案。
3. 扩展自动化用例到“跨页传递链路 + 列表排序命令 + 文档转运回滚场景”。

## 约束

- 不改动原 Python 项目文件。
- 不改变 ffmpeg 命令参数构建逻辑。
- 不影响现有 5 个功能模块可用性。
- 遇到样式覆盖问题优先使用模板级样式修复，非必要不做页面硬编码。

## 完成定义（DoD）

- 提交前或用户明确要求时，`dotnet build -c Debug` 通过。
- 五大功能入口可点击运行，无启动崩溃。

- 变更与回归结果已写入交接日志。
